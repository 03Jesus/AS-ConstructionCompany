version: "3"

services:
  constructioncompany:
    image: constructioncompany-image:3.0
    build: .
    container_name: constructioncompany-app
    ports:
      - "8000:8000"
    volumes:
      - ./:/app